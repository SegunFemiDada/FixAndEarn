{
  "name": "fixandearn",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "pnpm -C apps/api dev",
    "build:api": "pnpm -C apps/api build",
    "test:api": "pnpm -C apps/api test",
    "test:api:cov": "pnpm -C apps/api test:cov",
    "test:api:e2e": "pnpm -C apps/api test:e2e",
    "lint:api": "pnpm -C apps/api lint"
  },
  "devDependencies": {
    "@types/paystack": "^2.0.5",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "next": "^15.4.1",
    "paystack": "^2.0.1"
  }
}
